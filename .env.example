# Django Secret Key
# Generate a new secret key using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# CRITICAL: Never commit the actual secret key to version control!
DJANGO_SECRET_KEY=k4h!m#a0ip@ba2()i8gzxzzkv+!4ktsq2=3xjhym0ndw8pf^5z

# Allowed Hosts (comma-separated, NO SPACES)
# Development: localhost,127.0.0.1
# Staging: staging.example.com,www.staging.example.com
# Production: example.com,www.example.com,api.example.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Django Settings Module (development/staging/production/test)
# Security: Only these values are allowed (validated in settings/__init__.py)
# - openclinic.settings.development
# - openclinic.settings.staging
# - openclinic.settings.production
# - openclinic.settings.test
# Default: openclinic.settings.production if not specified
DJANGO_SETTINGS_MODULE=openclinic.settings.development

# Logging Configuration (for production)
# Directory where log files will be stored (production only)
# Default: /var/log/openclinic
LOG_DIR=/var/log/openclinic

# Database Configuration (optional, defaults to SQLite3)
# Uncomment and configure for PostgreSQL/MySQL in production
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=openclinic
# DB_USER=openclinic_user
# DB_PASSWORD=your_secure_password
# DB_HOST=localhost
# DB_PORT=5432